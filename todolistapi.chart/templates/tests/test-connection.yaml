apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "todolistapi.chart.fullname" . }}-test-connection"
  labels:
    {{- include "todolistapi.chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "todolistapi.chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
